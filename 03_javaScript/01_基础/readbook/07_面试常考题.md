# 题目展示

```javascript
a)!![]					true,数组一定是真的，即使是空数组，它哲学上也是“真性”的。
b)!![123]				true,数组一定是真的，即使是空数组，它哲学上也是“真性”的。
c)!!''					false
d)'' == ''			true
e)[] == []   			false，数组是引用类型值。
f)NaN == NaN   		false，NaN是基本值，但是JS规定它不能自等。
g)'false' && true   true  非空字符串就是真性的，短路
h)3 === '3'			false，三个等号也要一起判断类型

```

```javascript
a)5 >= 1 + 4 && !5 + true * 3
综合运算顺序： 数学 → 关系 → 逻辑（非、与、或）
5 >= 5 && false + 3
true && 3
3
b)NaN == NaN && undefined > true || 13
false && false || 13
false  || 13

```

```javascript
if (a) {
    var a = 10;
}
console.log(a);
不报错，有预读阶段。预读阶段只提升定义，不提升值。相当于：
if(undefined){
进不来
}

答案：undefined





a = 2;
var a;
console.log(a);

输出2，不是空、不是undefined、不报错。
因为var会提升



 
a)isNaN('' - '')			// false
b)isNaN('啊' - '啊')		// true    
c)isNaN('12' - '8')			// false
d)isNaN('-9')				// false
e)isNaN('-9+3')				// true
f)isNaN('undefined')		// true
g)isNaN(undefined)			// true
h)isNaN('NaN')				// true
i)isNaN(0 / 0)				// true
j)isNaN(NaN - NaN)			// true

isNaN结果是true，就是所有被Number()转结果是NaN：
① 非纯数字字符串、且非非空
② undefined



a)13 && 8				8
b)3 && undefined		undefined
c)'undefined' && undefined		undefined
d)'undefined' || undefined		'undefined' 
e)NaN || NaN				NaN 
f)3 || undefined		3





var a = [];
var b = a;
a.push(1);
console.log(b);	[1]
a、b指向的是内存中同一个数组。




var a = 2;
var b = 3;
var c = a++ + ++b + ++a + b++;
console.log(c);
面试题原题，14




a)parseInt(3.89)		3
b)parseInt('3年')		3
c)parseInt('有3年')	NaN
d)parseInt('100', 2)		4
e)Math.floor(-3.8)		-4
f)Math.floor(3.8)    3
g)parseInt(-3.8)   -3
h)parseInt(3.8)    3
i)Math.round(3.8)  4
j)Math.round(-3.8)   -4
```
